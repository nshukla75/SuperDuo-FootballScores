<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    android:id="@+id/message_view"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="@dimen/content_padding_normal"
    tools:ignore="UseCompoundDrawables">

    <ImageView
        android:id="@+id/message_view_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/content_padding_large"
        android:contentDescription="@string/message_empty_image"
        android:tint="@color/secondary_text"
        tools:src="@drawable/ic_state_empty" />

    <TextView
        android:id="@+id/message_view_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium"
        android:textColor="@color/secondary_text"
        tools:text="@string/scores_empty_message" />

</LinearLayout>